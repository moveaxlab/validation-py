{
  "rules": ["nullable"],
  "schema": {
    "a": {
      "rules": "minlen:8|maxlen:30|regex:[ab]+[cd]{2,4}|required",
      "type": "string"
    },
    "b": {
      "rules": [
        "required"
      ],
      "type": "uuid"
    },
    "c": {
      "rules": "maxlen:40|required",
      "type": "base64"
    },
    "d": {
      "rules": "maxlen:41|required",
      "type": "base58"
    },
    "e": {
      "rules": [
        "required"
      ],
      "type": "boolean"
    },
    "f": {
      "rules": "maxlen:30|required|nullable",
      "type": "email"
    },
    "g": {
      "rules": "max:100|required",
      "type": "integer"
    },
    "h": {
      "rules": "len:5|required",
      "type": "sequence"
    },
    "i": {
      "rules": "minlen:20|regex:^https|required",
      "type": "url"
    },
    "l": {
      "rules": "decimal|required",
      "type": "string"
    },
    "m": {
      "rules": "required|min:-2",
      "type": "float"
    }
  },
  "type": "object"
}